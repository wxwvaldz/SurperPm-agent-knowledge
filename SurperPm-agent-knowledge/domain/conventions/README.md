# conventions/

Team conventions — how we do things.

Examples:
- File naming
- Layering rules (e.g., "controllers never call DB directly")
- Test placement
- Commit message style

Edit by PR. Distilled additions arrive via PR.
