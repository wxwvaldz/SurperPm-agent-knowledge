# foundations/

Stable facts about the system that change rarely (yearly).

Examples:
- Tech stack choices and their rationale
- Public API contracts
- Auth model
- Data model fundamentals

Edit by PR. Distilled additions arrive via PR from the `distill` skill.
