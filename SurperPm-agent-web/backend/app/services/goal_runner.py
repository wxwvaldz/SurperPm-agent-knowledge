"""Goal lifecycle — provision sandbox, start CC, track state."""
# TODO (W2):
# - LAP: POST /pods, mount session + Repo 1, start CC headless
# - Local: git worktree + spawn `claude` subprocess
# - Track state in JSON file (no DB)
